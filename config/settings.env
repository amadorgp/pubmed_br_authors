# Identificação recomendada pelo NCBI (boa prática)
NCBI_EMAIL=
NCBI_TOOL=pubmed_br_authors_level1

# Opcional: API key do NCBI (gratuita). Se não tiver, deixe vazio por enquanto.
NCBI_API_KEY=

# Parâmetros padrão do pipeline
RETMAX=200
TIMEOUT_SECONDS=30
